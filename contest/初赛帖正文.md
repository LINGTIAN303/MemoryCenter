# 初赛帖正文（复制到 TRAE 社区发帖）

> 标签：学习工作
> 话题：#TRAE AI 创造力大赛 #vibe coding 大赏

---

## 标题

学习工作丨MemoryCenter —— 给 AI Agent 装一块"记忆硬盘"

## 一、创意介绍

**想解决什么问题**：
LLM 虽强，但每次对话结束即"失忆"。开发者反复向 Agent 解释项目背景，跨工具协作时记忆无法迁移，长项目里关键决策被遗忘。这不是模型能力问题，是记忆基础设施缺失。

**产品形态**：
MemoryCenter 是一个给 AI Agent 用的"记忆硬盘"——Rust 单二进制 + MCP Server。开发者配置一行 MCP 地址即可接入，Agent 自动获得跨会话的持久记忆能力：自动归档对话、按需检索历史、智能淘汰过时信息。

**目标用户**：AI 应用开发者、多 Agent 协作者、长期项目开发者

**用户痛点**：
- 跨会话失忆：每次新会话从头解释项目背景，重复消耗 token 与时间
- 多 Agent 孤岛：Trae / Claude Code / Cursor 间上下文彼此不通
- 长项目记忆超载：200K 上下文窗口放不下整个项目历史

## 二、Demo 立即体验

**在线 Demo**：http://mc-demo.lingtian.abrdns.com

**认证方式**：`Authorization: Bearer trae-contest-demo-key-2026`

### 评委快速验证（复制即用）

```bash
# 1. 健康检查
curl http://mc-demo.lingtian.abrdns.com/healthz

# 2. 查看归档记忆列表
curl -H "Authorization: Bearer trae-contest-demo-key-2026" \
  http://mc-demo.lingtian.abrdns.com/api/v1/sessions/cross-agent-demo-20260711/summaries

# 3. 语义检索（验证记忆召回能力）
curl -X POST -H "Authorization: Bearer trae-contest-demo-key-2026" \
  -H "Content-Type: application/json" \
  -d '{"query":"MemoryCenter 技术栈","top_k":5}' \
  http://mc-demo.lingtian.abrdns.com/api/v1/sessions/cross-agent-demo-20260711/search

# 4. 获取记忆摘要（模拟 Agent 新会话拉取历史记忆）
curl -H "Authorization: Bearer trae-contest-demo-key-2026" \
  http://mc-demo.lingtian.abrdns.com/api/v1/sessions/cross-agent-demo-20260711/prompt
```

### 跨 Agent 记忆迁移演示

Demo 环境预置了一条跨 Agent 记忆迁移记录：**Trae 归档的项目记忆 → Claude Code 通过同一 session_id 召回**。评委执行上面的 `prompt` 接口即可看到 Trae 归档的内容被完整召回。

## 三、TRAE IDE 开发说明

本项目使用 **TRAE IDE** 完成核心模块的原型开发与迭代：

- **场景预设扩充**：在 TRAE IDE 中新增 3 个参赛场景（Agent 协作 / 知识库 / 长项目），配置场景特化的归档阈值、检索策略、摘要 focus
- **Dashboard CLI**：用 TRAE IDE 开发 `mc-dashboard` 命令行工具，ratatui TUI 4 Tab 界面（概览 / 记忆列表 / 检索 demo / 评测对比）
- **跨 Agent 演示脚本**：在 TRAE IDE 中编写并验证跨 Agent 记忆迁移流程脚本
- **部署调试**：使用 TRAE IDE 的终端能力完成服务器部署、nginx 配置、端到端验证

## 四、核心能力

### 1. 自动归档 —— 无需手动整理
Agent 对话过程中，MemoryCenter 自动归档完整对话内容（含思考过程、工具调用、附件），不需要用户手动保存笔记。

### 2. 按需检索 —— 精准命中证据
支持语义检索 + 关键词检索的混合模式。LLM 通过 `semantic_search` 工具即可召回相关历史记忆，直接注入当前对话。

### 3. 跨 Agent 迁移 —— 记忆不再孤立
基于 session_id 命名空间设计，不同 Agent 工具（Trae / Claude Code / Cursor）可通过同一 session_id 共享记忆。

### 4. 智能淘汰 —— 记忆不会膨胀
三级索引周期（天/周/月）+ 4 维加权评分，自动淘汰低价值记忆，类比人脑"记忆巩固"机制。

## 五、评测数据

### 1. LoCoMo 长时记忆基准（纯算法评分）

| 模型 | baseline F1 | MemoryCenter F1 | 提升 |
|------|-------------|----------|------|
| SenseNova | 0.1036 | **0.1465** | **+41.4%** |
| Step | 0.1105 | **0.1345** | **+21.7%** |

### 2. LongMemEval 检索评测（500 题数据集 30 题抽样）

| 指标 | 结果 |
|------|------|
| recall@5 | **100%**（30/30 命中） |
| nDCG@5 | 0.9405 |

### 3. 上下文压缩优势

| 条件 | 耗时 | 说明 |
|------|------|------|
| baseline | 127.1s | 全量对话历史发送给 LLM |
| MemoryCenter | 87.6s | 归档 + 摘要 + 检索注入 |
| **速度提升** | **31%** | 上下文压缩带来显著加速 |

## 六、用户价值

**对开发者**：
- 接入零成本：配置一行 MCP 地址，无需改代码
- 节省 token：跨会话记忆复用，不用重复解释项目背景
- 长项目可控：关键决策、踩坑经验永久留存

**对 Agent 生态**：
- 打破 Agent 孤岛：不同工具间记忆可迁移
- 长期记忆：突破 200K 上下文窗口限制
- 自治记忆：Agent 自动归档/检索/淘汰，无需人工干预

## 七、代码与协议

- **代码仓库**：https://github.com/LINGTIAN303/MemoryCenter
- **协议**：MIT 开源

## 八、致谢

感谢 TRAE IDE 团队提供的开发环境，以及 SenseNova / Step / Qwen / SiliconFlow 等 API 提供方支持的评测资源。

---

## 发帖检查清单

- [ ] 类别选择「TRAE AI 创造力大赛 - 初赛专区」
- [ ] 标签选择「学习工作」
- [ ] 话题添加 #TRAE AI 创造力大赛 #vibe coding 大赏
- [ ] 标题：学习工作丨MemoryCenter —— 给 AI Agent 装一块"记忆硬盘"
- [ ] 正文包含：创意介绍 / Demo 体验 / TRAE 开发说明 / 核心能力 / 评测数据 / 用户价值
- [ ] 确认 TRAE 中国版与社区注册使用同一手机号
